name: Terraform Plan

on:
  push:
    branches:
      - main  # Change this to your main branch name if different

jobs:
  terraform:
    runs-on: ubuntu-latest
    steps:

    - name: Checkout code
      uses: actions/checkout@v2

    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: AKIAUPDHFUQPAJFOZYMC
        aws-secret-access-key: 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
        aws-region: us-east-1

    - name: AWS 
      run: aws s3 ls
      shell: bash
